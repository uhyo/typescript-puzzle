const prefix = "type-puzzles-";

/**
 * LocalStorage key for whether the user closed the notice for use of Cookie and Google Analysis.
 */
export const privacyConfirmationKey = `${prefix}privacy-confirmation`;

/**
 * LocalStorage key for the number of questions last seen by this user.
 */
export const lastSeenQuestionNumberKey = `${prefix}last-seen-question-number`;
