import styled from "styled-components";
import { warningBackgroundColor } from "~/design/color";

export const NoticeBox = styled.div`
  margin: 4px 0;
  padding: 4px;
  background-color: ${warningBackgroundColor};
`;
